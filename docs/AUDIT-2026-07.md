# Audit — juli 2026 (etter funksjonsbølgen)

Fersk gjennomgang etter alt som er bygget siden forrige audit: CORS, CSP (Report-Only),
«Min side», konto-bøttelagring, integritetsskala/POMP, rolleprofiler (revidert),
analytics, AI-samtykke, PWA, gruppert meny, fargeopprydding, modellbytte til
gemini-3-flash-preview. Metode: målrettet kodegjennomgang + npm audit + verifisering
av kjente invariante egenskaper (auth-dekning, idempotent kreditering, bøttelogikk).

## Status (oppdatert 2026-07-02)
Alt som kunne lukkes uten bruker-input er nå lukket:
- **A1 — LUKKET:** CSP håndheves i prod (rev 00021). Verifisert ren konsoll gjennom
  innlogging + kjøpsmodal + AI-analyse i Report-Only-fasen før flipp til `false`.
- **A2 — LUKKET:** `GEMINI_MODEL` env-var + automatisk fallback til `gemini-2.5-flash`
  ved 404 (rev 00021, `generateWithFallback` i server.ts).
- **A5 — LUKKET:** `todayKey()` bruker nå Europe/Oslo (rev 00022).
- **A3 — ÅPEN (kun bruker):** eneste gjenstående blokker for salg. Se under.
- **A4, A6 — ÅPEN, ingen handling:** bevisst akseptert / venter på upstream.

Kun **A3** blokkerer nå lansering, og den krever data bare eieren har.

## Bekreftet i orden (nytt siden sist)

- CORS låst til APP_URL; admin-endepunkt rate-limited; Stripe promo-codes aktivert.
- `/api/track`: allow-liste + rate-limit, aggregert per dag, ingen bruker-ID — i tråd
  med «100 % lokalt»-løftet. Kjøp telles idempotent inne i webhook-transaksjonen.
- Konto-bøttelagring: `bigfive_prep_score_history` og `bigfive_prep_ai_consent` er
  begge med i STORAGE_KEYS → følger backup/gjenoppretting OG slettes korrekt ved
  GDPR-sletting/reset. Logikken er scenario-testet (6 tilfeller).
- AI-samtykke gates begge AI-funksjonene; eksport/import og PDF påvirkes ikke.
- Kontrastfiks på gull-badges (WCAG ≥ 4.5:1).

## Funn

### A1 · HØY — ✅ LUKKET (rev 00021)
CSP flippet fra Report-Only til håndhevende etter verifisert ren konsoll gjennom
innlogging + kjøpsmodal + AI-analyse. XSS-forsvaret er nå reelt på; `upgrade-insecure-
requests` trer også i kraft. Endres policyen senere: kjør alltid en ny Report-Only-
runde først (feil CSP knekker innlogging stille).

### A2 · MEDIUM — ✅ LUKKET (rev 00021)
Modellnavn styres av `GEMINI_MODEL` (default `gemini-3-flash-preview`).
`generateWithFallback()` bytter varig (per instans) til `gemini-2.5-flash` ved 404, og
prøver kallet på nytt — bare første kall betaler dobbel latens. Forsvinner preview-
modellen, fortsetter begge betalte funksjoner å virke.

### A3 · MEDIUM (venter på deg) — Salg kan ikke aktiveres ennå
Uendret siden sist: `[Selskapsnavn]`, org.nr, e-post og dato er ufylt i
PERSONVERN.md/VILKAR.md; `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` og
`RESEND_API_KEY`/`EMAIL_FROM` mangler i .env. Koden er klar; dette er de eneste
blokkerne for lovlig, fungerende salg.

### A4 · LAV — /api/track kan støyes av en motivert aktør
Uautentisert skriving (bevisst — anonyme skal telles), begrenset til 10/min/IP per
instans (×5 instanser). En bot kan blåse opp tallene/skape marginale skrivekostnader,
men kan ikke skrive vilkårlige felt. Akseptabel risiko for aggregert statistikk.

### A5 · LAV — ✅ LUKKET (rev 00022)
`todayKey()` bruker nå `Intl.DateTimeFormat("en-CA", { timeZone: "Europe/Oslo" })` —
statistikk-døgnet skifter ved midnatt norsk tid.

### A6 · LAV — npm audit: 6 moderate (uendret)
Transitiv `uuid` via firebase-admin; ikke utnyttbar i vår bruk; venter på upstream.

### Info
- In-memory rate-limiter per instans: kjent, bevisst akseptert (budsjett-tak er backstop).
- `server.ts` er nå ~950 linjer; vurder modulsplitting ved neste større serverarbeid.
- AI-samtykket håndheves kun klient-side. Greit: API-kalleren ER datasubjektet, og
  dermed kan ingen andres data sendes uten deres handling — men dokumentert her.
- Eldre audit-dokumenter (AUDIT-2026-06*) beskriver ikke ny funksjonalitet.

## Etterarbeid siden auditen (ikke funn — nye leveranser)
- Ytelse: bundle-splitt (hovedchunk 619 KB → app-kjerne 134 KB; firebase/react i egne
  cachede vendor-chunks; lazy-lastede seksjoner) — rev 00023.
- Klient-feilteller (`client_error`, aggregert, én gang per økt) — rev 00023.
- Nye funksjoner: retake-knapp, intervju-tilbakemelding, admin-analytics-endepunkt,
  20-klipps pakke — rev 00022.

## Prioritert handlingsliste (gjenstår)
1. **A3 (kun bruker):** Org.nr/selskapsinfo i PERSONVERN.md/VILKAR.md + Stripe-nøkler
   (aktiverer salg) + ev. Resend-nøkkel (ekte kvittering). Eneste lanseringsblokker.
2. A4/A6: ingen handling nå.
3. Vurder ved neste serverjobb: modulsplitting av `server.ts`; delt rate-limiter hvis
   trafikken vokser.
